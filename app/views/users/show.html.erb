<div class="top-wrapper"> 
  <h2>マイページ</h2>
  <span class="name">名前：<%= @user.name %></span>
  <%= link_to edit_user_registration_path,class: 'profile-edit-btn' do %>
    <span>プロフィールの編集</span>  
  <% end %>
</div>

<table class="table">
  <tr>
    <td><%= image_tag current_user.avatar_url, class: 'rounded-circle avatar-image' %></td>
  </tr>
</table>
